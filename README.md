# How to run assignment
1. clone repo
2. open terminal inside project root
3. run the command: ```docker compose up --build```
4. wait for docker to finish booting all 3 services
   - mongo
   - frontend
   - backend
5. open browser and go to: ```http://localhost:3000```
